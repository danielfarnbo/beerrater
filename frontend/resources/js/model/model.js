var BeerModel = Backbone.Model.extend({	populate: function(beer){		this.set({name:beer.name, picture:beer.picture, abv:beer.abv, brewery:beer.brewery, beernr:beer.beernr, _id:beer._id } );	}});var BeerCollection = Backbone.Collection.extend({	populateCollection: function(beerList){		for(var i=0; i < beerList.length; i++)		{			var model = new BeerModel();			model.populate(beerList[i]);			this.add(model);		}				console.log(window.beerCollection.at(0).get("name"));	}});