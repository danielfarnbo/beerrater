var collection;var IndexView = Backbone.View.extend({	initialize: function () {		this.model.bind('change:rating', this.render, this);	},	render: function(){		var view = this;		this.$el.empty();				for( var i = 0; i < this.model.length; i++ )		{			var model = this.model.at(i);			model.set('ratingcls', model.get('rating') ? 'rated' : 'unrated');			var template = _.template( $("#index_template").html(), model.attributes );						// Load the compiled HTML into the Backbone "el"			this.$el.append( template );		}		return this;	},	events:{		"click a": "goToBeer"	},		goToBeer:function(event){		var index = $(event.currentTarget).attr("beerIndex");		this.model.setCurrentBeer(index);	}});