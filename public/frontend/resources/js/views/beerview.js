var collection;var BeerView = Backbone.View.extend({		render:function(model){				var self = this,			ratings = new Array(100),			select;		for (var i = 1; i<=100; i++){			ratings[i-1] = i;		}		model.attributes.ratings = ratings;				var template = _.template( $('#presentation_template').html(), model.attributes );		this.$el.html(template);		select = $('#rating-' + model.attributes.beernr);		select.change(function(){            console.log('rating:', select.val());        });				$('#nextArrow').click(function(){			self.trigger('nextBeer');		});				$('#prevArrow').click(function(){			self.trigger('prevBeer');		});				$('#back').click(function(){			self.trigger('back');		});	}});